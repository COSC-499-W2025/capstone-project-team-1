[project]
name = "artifactminer"
version = "0.1.0"
description = "Artifact mining system for repository intelligence"
readme = "../README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "gitpython"
]
